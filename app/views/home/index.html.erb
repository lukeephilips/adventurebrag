<h1>AdventureBrags</h1>
<div class="row">
  <div class="col s8 offset-s2">
    <div class="carousel carousel-slider">
      <% Place.all.each do |place| %>
        <%= link_to image_tag(place.image, class: 'carousel-item'), place_path(place) %>
      <% end %>
    </div>
  </div>
</div>
<h3>AdventureBrags is a site to brag about the adventures you go on. Login to contribute, or click an adventure to view</h3>
